# Strip the `/v1` suffix to get the server base URL (for `/tokenize`)

Strip the `/v1` suffix to get the server base URL (for `/tokenize`)

## Usage

``` r
server_url(base_url)
```

## Arguments

- base_url:

  Character. Base URL with `/v1`.

## Value

Character. Base URL without `/v1`.
