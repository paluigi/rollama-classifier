# Build JSON schema for constrained output

Build JSON schema for constrained output

## Usage

``` r
build_json_schema(labels)
```

## Arguments

- labels:

  Character vector of valid labels.

## Value

A list representing a JSON schema.
