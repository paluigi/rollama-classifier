# Get scored lengths per label

Returns the number of tokens scored per label based on the divergence
point.

## Usage

``` r
get_scored_lengths(token_sequences, winning_label)
```

## Arguments

- token_sequences:

  Named list.

- winning_label:

  Character.

## Value

Named integer vector.
