# Build JSON enum schema for label constraint

Build JSON enum schema for label constraint

## Usage

``` r
build_json_enum(labels)
```

## Arguments

- labels:

  Character vector of valid labels.

## Value

A list representing a JSON schema with enum constraint.
