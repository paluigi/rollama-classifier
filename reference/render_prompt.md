# Render messages to a plain text prompt for completions endpoint

Render messages to a plain text prompt for completions endpoint

## Usage

``` r
render_prompt(messages)
```

## Arguments

- messages:

  List of message lists with `role` and `content`.

## Value

Character string.
