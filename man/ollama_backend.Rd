% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backends.R
\name{ollama_backend}
\alias{ollama_backend}
\title{Ollama Backend}
\usage{
ollama_backend(
  model,
  host = "http://localhost:11434",
  timeout = 120,
  max_tokens = 256,
  extra_body = list()
)
}
\arguments{
\item{model}{Character. Model name (e.g., \code{"llama3.2"}).}

\item{host}{Character. Ollama server URL. Defaults to
\code{"http://localhost:11434"}.}

\item{timeout}{Numeric. Request timeout in seconds.}

\item{max_tokens}{Integer. Max tokens to generate.}

\item{extra_body}{List. Extra parameters for options.}
}
\value{
A backend list with \code{chat()}, \code{score()}, \code{tokenize()}, and
capability flags.
}
\description{
Backend for the Ollama runtime (>=v0.12) via the native Ollama REST API.

Ollama uses JSON Schema enum for label constraints. The model generates
\code{{"label": "<chosen>"}}. Structural JSON tokens are filtered during trie
reconstruction and completion scoring.

Modern Ollama removed the \verb{/api/tokenize} endpoint and does not support
fill-in-the-middle ("insert") on instruct models. This backend therefore
obtains both label tokenization and completion scores through empirical
\emph{forced constrained generation} (forcing a label as the only valid choice
and reading back the model's genuine per-token logprobs). No
\verb{/api/tokenize} or \code{suffix}/insert calls are used. Tokenization results
are memoized per label.
}
\examples{
\dontrun{
backend <- ollama_backend("llama3.2")
classifier <- llm_classifier(backend)
}
}
