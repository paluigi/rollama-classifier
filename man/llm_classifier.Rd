% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifier.R
\name{llm_classifier}
\alias{llm_classifier}
\title{Unified LLM Classifier}
\usage{
llm_classifier(backend)
}
\arguments{
\item{backend}{A backend object created by \code{\link[=ollama_backend]{ollama_backend()}},
\code{\link[=vllm_backend]{vllm_backend()}}, \code{\link[=sglang_backend]{sglang_backend()}}, or \code{\link[=llamacpp_backend]{llamacpp_backend()}}.}
}
\value{
A classifier object (list of closures) usable with the S3 generics
\code{\link[=generate]{generate()}}, \code{\link[=classify]{classify()}}, \code{\link[=batch_generate]{batch_generate()}}, \code{\link[=batch_classify]{batch_classify()}}.
}
\description{
Create a backend-agnostic classifier with two confidence scoring methods:
\itemize{
\item \code{generate()}: Adaptive constrained generation with divergence-aware
confidence. Budget-controlled via \code{max_calls}. Makes 1 to \code{max_calls}
constrained API calls.
\item \code{classify()}: Multi-call completion scoring with geometric-mean
normalization. Always exact. Makes N calls for N labels.
}
}
\examples{
\dontrun{
backend <- ollama_backend("llama3.2")
classifier <- llm_classifier(backend)

result <- classify(
  classifier,
  text = "I love this product!",
  choices = c("positive", "negative", "neutral")
)
print(result$prediction)
print(result$confidence)
}
}
