% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama-api.R
\name{ollama_label_token_logprobs}
\alias{ollama_label_token_logprobs}
\title{Extract label-value tokens from a JSON-enum constrained response}
\usage{
ollama_label_token_logprobs(logprobs, label)
}
\arguments{
\item{logprobs}{List of logprob entries, each with \code{token}, \code{logprob},
and \code{top_logprobs}.}

\item{label}{Character. The label text to locate.}
}
\value{
A filtered list of logprob entries (only the label-value tokens).
}
\description{
Extracts the label-value tokens (with their logprobs) from a
\code{{"label": "<label>"}} constrained response.
}
\details{
Robust to model-specific whitespace in the emitted JSON. The returned
tokens keep their \emph{exact} emitted strings so they match the tokens the
model produces during multi-label constrained generation in \code{generate()}.

Primary strategy: reconstruct the full emitted string, locate the value
span after the JSON \code{:} separator, and map that character span back to
token indices. Falls back to JSON-skeleton filtering if the span mapping
yields nothing.
}
\keyword{internal}
